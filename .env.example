ENVIRONMENT=development
# python3 -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-at-least-32-characters-long-generate-with-command-above
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=app
FIRST_SUPERUSER=admin
FIRST_SUPERUSER_PASSWORD=Admin1234
FIRST_SUPERUSER_FULL_NAME=Admin User
# 反向代理可信 IP（逗号分隔），用于正确解析 X-Forwarded-For
TRUSTED_HOSTS=127.0.0.1
REDIS_PASSWORD=changethis
REDIS_URL=redis://:changethis@localhost:6379/0
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:5173
